import { gsap } from 'gsap';

// Function to animate guide button entrance - keeping the function to avoid breaking imports
// but removing all implementation (it's now a no-op)
export const animateGuideButtonEntrance = (buttonRef: HTMLElement | null) => {
  // Functionality removed
};
